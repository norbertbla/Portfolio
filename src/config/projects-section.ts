import { ProjectsSectionConfig } from "types";

export const projectsSectionConfig: ProjectsSectionConfig = {
  title: "Projects",
  paragraph:
    "I've built a couple of projects in the past but only recently started enjoying contributing to Open Source, currently I'm working on Booksly a book aggregator app that uses the improved openlibrary database to sort, rank and filter books.",
};
